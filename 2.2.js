let min = prompt ("saisir un nombre minimum");
let max = prompt ( "saisir un nombre maximum");
let current = prompt ("saisir un nombre aléatoire");

if (current > min && current < max) {
    console.log( ' ce nombre ce situe entre les deux précédents');
     
}
